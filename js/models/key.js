var app = app || {};
/*$(function() {
  app.Key = Backbone.PubNub.Model.extend({
    name: 'MyModelKey',
    pubnub: pubnub,
    defaults: {
      title: "the key",
      key: [0,5,10]
    }
  });
app.key = new app.Key();
});
*/
